[project]
name = "api"
version = "0.1.0"
description = "Simple FastAPI demo with hello world and health endpoints"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.141.1",
    "uvicorn>=0.52.4",
]
